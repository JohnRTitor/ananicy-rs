.*
!.gitignore
!.github
!.agents
# Cargo output of every crate, including the separate `fuzz` workspace.
target/
# The fuzz targets are an opt-in development tool, resolved on demand.
crates/ananicy-platform/fuzz/Cargo.lock
result
